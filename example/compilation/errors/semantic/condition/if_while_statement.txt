PROG

var x : int;
var is_red : bool;

x := 1;

// Error: Cannot compare bool and bool with '>' operator
if ((x > 5) || (is_red >= false)) {
    x := 0;
}

// Error: x is an int, not a float
while(x < 5.0) {
    x := x + 1;
}
